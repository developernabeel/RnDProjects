<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <configSections>
    <!-- If Log4Net is not configured in code using XmlConfigurator then this can be used as an alternative, also need to add a tag in AssemblyInfo.cs -->
    <!--<section name="log4net"
    type="log4net.Config.Log4NetConfigurationSectionHandler,Log4net"/>-->
  </configSections>

  <!-- Alternate to XmlConfigurator -->
  <!--<log4net configSource="Log4Net.config" />-->
    <startup> 
        <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.5" />
    </startup>
</configuration>