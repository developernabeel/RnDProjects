@model SBIReportUtility.Entities.UserModel
@using SBIReportUtility.Common.General;

<div class="navbar-header">
    @*<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
    </button>*@
    @if (Model.RoleId == (int)EnumHelper.Role.SuperAdmin)
    {
        <a class="navbar-brand" href="@Url.Action("Index", "Home")">State Bank of India</a>
    }
    else
    {
        <a class="navbar-brand" href="@Url.Action("Dashboard", "Project")">State Bank of India</a>
    }
    
</div>
<!-- /.navbar-header -->
<ul class="nav navbar-top-links navbar-right">
    <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#" aria-expanded="true">
            <i class="fa fa-user fa-fw"></i> <i class="fa fa-caret-down"></i>
        </a>
        <ul class="dropdown-menu dropdown-user">
            <li>
                <a href="@Url.Action("Logout","Account")"><i class="fa fa-sign-out fa-fw"></i> Logout</a>
            </li>
        </ul>
        <!-- /.dropdown-user -->
    </li>
</ul>