@model SBIReportUtility.Web.Models.Project.ProjectDetailsViewModel
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section ProjectMenu {
    @{ Html.RenderPartial("~/Views/Shared/_ProjectMenu.cshtml", Model); }
}

@Html.HiddenFor(m => m.ProjectId, new { @id = "hdnProjectId" })
<div class="row form-group">
    <h2>Project Details</h2>

    @*@if (Model.IsProjectAdmin)
    {
        <div class="row form-group pull-right">
            <div class="col-lg-12">
                <a href="@Url.Action("Index", "Report")?projectId=@Model.ProjectId" class="btn btn-primary">Add Report</a>
                <a href="@Url.Action("Index", "Report")?projectId=@Model.ProjectId" class="btn btn-primary">Assign Report</a>
                <a href="@Url.Action("Index", "Connection")?projectId=@Model.ProjectId" class="btn btn-primary">Add Connection</a>
            </div>
        </div>
    }*@
</div>

<div class="row">
    <div class="panel panel-default">
        <div class="panel-heading">
            <span>Project Info</span>
        </div>
        <div class="panel-body">

            <div class="form-group row">
                <div class="col-lg-2">
                    @Html.LabelFor(m => m.ProjectName) :
                </div>
                <div class="col-lg-10">
                    @Html.DisplayFor(m => m.ProjectName, new { @class = "" })
                    <div class="clearfix"></div>
                </div>
            </div>
            <div class="form-group row">
                <div class="col-lg-2">
                    @Html.LabelFor(m => m.Description) :
                </div>
                <div class="col-lg-10">
                    @Html.DisplayFor(m => m.Description, new { @class = "" })
                    <div class="clearfix"></div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="panel panel-default">
        <div class="panel-heading">
            <span>Project Reports</span>
        </div>
        <div class="panel-body">
            <div class="col-lg-12">
                <table width="100%" class="table table-striped table-bordered table-hover" id="tbl-reports" cellspacing="0"></table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/Scripts/CustomJs/Project/details.js"></script>
}


